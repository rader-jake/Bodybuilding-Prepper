## Packages
date-fns | Date formatting for check-ins
recharts | Charts for athlete weight/adherence tracking
framer-motion | Smooth transitions and animations for dashboard elements
lucide-react | Iconography (already in base, but listing for completeness)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
